# ArcReel is bound to 127.0.0.1 by compose.yml. Authentication remains off
# for this local-only sidecar; enable it before changing the port binding.
AUTH_ENABLED=false
AUTH_USERNAME=admin
AUTH_PASSWORD=
AUTH_TOKEN_SECRET=
LOG_LEVEL=INFO
TZ=Asia/Shanghai
